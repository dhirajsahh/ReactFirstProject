import React from "react";
import { useState } from "react";

function Button() {
  return (
    <div>
      <button onClick={() => {}} className="notinterested-btn">
        Not interested
      </button>
    </div>
  );
}

export default Button;
